[tool.poetry]
name = "python_test"
version = "0.1.0"
description = ""
authors = ["jamesh4 <jamesh4@alumni.ubc.ca>"]

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
